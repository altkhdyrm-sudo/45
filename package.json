{
  "name": "madrasati-static-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host 0.0.0.0",
    "lint": "echo 'No linting needed for static assets'"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
